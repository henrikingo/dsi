# Shared by all mongods
mongod_config_file:
  storage:
    engine: wiredTiger
    id: paula

topology:
  - cluster_type: standalone
    private_ip: 1
    id: ${bootstrap.owner}

# Meta data about this mongodb setup
meta:
  port: 27017
  test: data
