{
  "$jsonSchema": {
    "bsonType": "object",
    "required": [
      "all_suspect_revisions"
    ],
    "properties": {
      "all_suspect_revisions": {
        "bsonType": "array",
        "items": {
          "type": "string"
        },
        "minItems": 1,
        "description": "must be an array of strings with at least one element"
      }
    }
  }
}